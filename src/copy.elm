module Copy exposing (..)

title = "Conway's Game of Life"

clear = "Clear"
pause = "Pause"
unpause = "Unpause"

population = "Population"

rules = "Rules"
aliveRule = "A live cell stays alive if it has 2 or 3 live neighbors"
deadRule = "A dead cell comes to life if it has exactly 3 live neighbors"

saveBoard = "Save board"
